apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00028
  namespace: sno00028
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00028
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00028
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00028
  pullSecretRef:
    name: assisted-deployment-pull-secret
