apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00033
  namespace: sno00033
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00033
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00033
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00033
  pullSecretRef:
    name: assisted-deployment-pull-secret
