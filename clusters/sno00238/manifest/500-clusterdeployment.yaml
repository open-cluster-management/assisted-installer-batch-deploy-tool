apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00238
  namespace: sno00238
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00238
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00238
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00238
  pullSecretRef:
    name: assisted-deployment-pull-secret
