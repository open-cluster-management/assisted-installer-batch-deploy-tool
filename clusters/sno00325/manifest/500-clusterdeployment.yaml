apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00325
  namespace: sno00325
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00325
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00325
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00325
  pullSecretRef:
    name: assisted-deployment-pull-secret
