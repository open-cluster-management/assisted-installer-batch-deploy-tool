apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00386
  namespace: sno00386
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00386
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00386
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00386
  pullSecretRef:
    name: assisted-deployment-pull-secret
