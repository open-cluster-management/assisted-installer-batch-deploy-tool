apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00400
  namespace: sno00400
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00400
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00400
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00400
  pullSecretRef:
    name: assisted-deployment-pull-secret
