apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00506
  namespace: sno00506
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00506
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00506
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00506
  pullSecretRef:
    name: assisted-deployment-pull-secret
