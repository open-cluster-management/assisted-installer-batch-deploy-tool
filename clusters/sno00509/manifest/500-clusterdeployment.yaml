apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00509
  namespace: sno00509
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00509
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00509
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00509
  pullSecretRef:
    name: assisted-deployment-pull-secret
