apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00511
  namespace: sno00511
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00511
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00511
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00511
  pullSecretRef:
    name: assisted-deployment-pull-secret
