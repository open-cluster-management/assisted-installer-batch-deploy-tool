apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00665
  namespace: sno00665
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00665
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00665
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00665
  pullSecretRef:
    name: assisted-deployment-pull-secret
