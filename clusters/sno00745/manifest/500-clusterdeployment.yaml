apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno00745
  namespace: sno00745
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno00745
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno00745
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno00745
  pullSecretRef:
    name: assisted-deployment-pull-secret
