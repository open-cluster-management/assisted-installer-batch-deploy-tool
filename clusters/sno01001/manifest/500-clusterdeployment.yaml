apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno01001
  namespace: sno01001
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno01001
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno01001
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno01001
  pullSecretRef:
    name: assisted-deployment-pull-secret
