apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno01064
  namespace: sno01064
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno01064
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno01064
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno01064
  pullSecretRef:
    name: assisted-deployment-pull-secret
