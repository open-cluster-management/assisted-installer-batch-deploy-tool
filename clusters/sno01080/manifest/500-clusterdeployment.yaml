apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: sno01080
  namespace: sno01080
spec:
  baseDomain: rdu2.scalelab.redhat.com
  clusterName: sno01080
  clusterInstallRef:
    group: extensions.hive.openshift.io
    version: v1beta1
    kind: AgentClusterInstall
    name: sno01080
  platform:
    agentBareMetal:
      agentSelector:
        matchLabels:
          cluster-name: sno01080
  pullSecretRef:
    name: assisted-deployment-pull-secret
