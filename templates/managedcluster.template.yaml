apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{CLUSTER_NAME}}
  labels:
    name: {{CLUSTER_NAME}}
    vendor: OpenShift
    {{OBSERVABILITY_LABEL}}
    common: 'true'
    group-sno-du: 'true'
spec:
  hubAcceptsClient: true
